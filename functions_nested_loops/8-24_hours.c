#include "main.h"
/**
 * jack_bauer - prints every minute of the day
 * @n: The number to check
 *
 * Return: nothing
 */

void jack_bauer(void)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (n * (-1));
	}
	else
	{
		return (0);
	}
}
