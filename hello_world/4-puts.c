#include <stdio.h>
/**
 * main- entry point
 *
 * return- always zero to signify success
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	/**
	 * return - always zero to show success
	 */
	return (0); }
